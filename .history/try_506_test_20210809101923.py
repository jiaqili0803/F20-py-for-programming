def total_len(str_list):
    """
    Parameters
    ----------
    strings
        a list of strings

    Returns
    -------
        the total length of every string in
        that list added together
    """
